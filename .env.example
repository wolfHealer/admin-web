# 开发 (npm run dev) 与生产构建 (npm run build) 都会读取本文件
# 部署前在此改成服务器上的 API 地址，再执行 npm run build
#
# 本地开发示例：
# VITE_API_BASE_URL=http://localhost:8080/api
#
# ECS 同域 Nginx 反代 /api 示例：
# VITE_API_BASE_URL=/api
#
# 个人本地覆盖（不提交 git）：复制为 .env.local

VITE_API_BASE_URL=http://localhost:8080/api
